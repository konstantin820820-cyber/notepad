name: notepad

description: A Flutter note app.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  shared_preferences: ^2.5.3
  fleather: ^1.27.0
  reorderable_grid_view: ^2.2.8
  flutter_file_dialog: ^3.0.2
